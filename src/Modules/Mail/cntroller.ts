import { NextFunction, Request, Response } from "express";



export async function sendSignUPLink(req: Request, res: Response, next: NextFunction) {
    try {

    } catch (error) {
        next(error);
    }
}